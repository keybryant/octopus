# octopus-workflow

Agent workflow orchestration plugin for the octopus workbench.

Shared structural types (`TaskSessionEvent`, store/session `*Like` interfaces, `WorkflowError`) for the task-session manager and workflow tools.
